// peek.nc — a descent that asks the world nothing.
//
// `descend disk` holds the disk for the length of the block. The block reaches
// no prelude function, so there is nothing for a grant to answer and burial
// evaluates it with no capability held at all. spec/06-evaluation.md §6.1.
demand descend disk { 1 + 1 };
